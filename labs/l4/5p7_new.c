#include <stdio.h>


int main(void) {

	int i,i1, i2, i3, i4, min_one, min_two, min, max_one, max_two, max;

	printf("Enter four digits: ");
	for (i=0; i < 4; i++) {
		scanf("%d", &i1);
		if (i==0) {
			max = i1;
			min = i1;
		} else if (i1 > max) {
			max = i1;
		} else if (i1 < min) {
	}	

		return 0;


}
